engine:
  name: "sglang"
  command: "python3 -m sglang.launch_server"

server:
  host: "0.0.0.0"
  port: 8000
  random_seed: 42
  trust_remote_code: true

memory:
  mem_fraction_static: 0.90
  kv_cache_dtype: "auto"

performance:
  chunked_prefill_size: 8192
  max_running_requests: 4
  schedule_policy: "lpm"

tool_calling:
  tool_call_parser: "qwen"

# thinking mode: controlled per-request via chat_template_kwargs in API body
# pass {"chat_template_kwargs": {"enable_thinking": false}} to disable
chat_template: {}

parallelism:
  tensor_parallel_size: 1

logging:
  log_level: "info"
